#ifdef COSINUS__HPP__
#define COSINUS__HPP__

/* 
#include <factorielle.hpp>
#include <puissance.hpp>


template <unsigned long N> struct Cosinus{
    static double valeur(double x) {
        return Cosinus<N-1>::valeur(x) + (Puissance<N>::valeur(-1) * Puissance<2N>::valeur(x)) / Factorielle<2N>::valeur(x);
    }
};

template<> struct Cosinus<0>{
    static double valeur(double ) {
        return 1.0;
    }
} ;

template <unsigned long N> struct Sinus{
    static double valeur(double x) {
        return Sinus<N-1>::valeur(x) + (Puissance<N>::valeur(-1) * Puissance<2N+1>::valeur(x)) / Factorielle<2N+1>::valeur(x);
    }
} ;

template<> struct Sinus<0>{
    static double valeur(double x) {
        return x;
    }
} ;
*/
#endif